#include <string>
#include <iostream>
#include <cmath>
int main()
{
std::cout << " vvedite chisla\n ";
    int A,B;
    std::cin >> A >> B;
    std::cout << sqrt(pow(A,2) + pow(B,2));
    return 0;
}
